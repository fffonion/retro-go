cmake_minimum_required(VERSION 3.5)
if(RG_ENABLE_NETWORKING)
    set(COMPONENTS "main retro-go wifi esp_http_server")
else()
    set(COMPONENTS "main retro-go")
endif()
include(../base.cmake)
project(launcher)
